package com.xero.invoice.core.utils

object InvoiceConstants {

    // Messages
    const val NO_INVOICES_MESSAGE = "No invoices found"

    // Formatting
    const val DEFAULT_CURRENCY_VALUE = "$0.00"

    // Validation
    const val MIN_INVOICE_ITEMS = 1
    const val MIN_PRICE_CENTS = 0
    const val MIN_QUANTITY = 1
}